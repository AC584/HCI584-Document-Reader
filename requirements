
* This program was built with Windows 10
* Libraries: Tkinter
* Modules: collections, os, re
* Packages:
    pandas>=2.2.1
    python-docx>=1.1.2
    PyPDF2>=3.0.1

For packages, you must first install the package (e.g., %pip install pandas). Once installed, you need to import the package in order to use it
